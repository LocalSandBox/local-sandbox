# Windows service implementation state

## Status

Phases 0-6 upstream shipping foundations are implemented. The initial service scope is deliberately mount-free and host-port-free; production signing, downstream installer integration, and the real-machine acceptance matrix remain explicitly gated.

- Windows standard-token gate refresh (2026-07-20): the source-only release/evidence suite passes all 25 `xtask` tests, and the retained redacted incomplete-profile manifest passes the real `verify-windows-evidence` shape/hash validator without modifying evidence. The service protocol and client suites pass 28 and 21 tests, the standalone Node binding passes 7 Rust tests, and the production proxy passes 79 tests covering public-route filtering, DNS pruning/rebinding, WPAD denial, IPv4/IPv6, direct-IP/host policy, scoped secret/header injection, and explicit-proxy credential isolation. These checks created no service, Job, account, share, ACE, WFP object, listener, relay connection, installer state, or machine policy. They do not satisfy the signed exact-artifact/full-profile, LocalSystem, privileged, multi-user, reboot, or managed-fleet gates.
- CON-01/SEC-02 production QEMU-ledger tranche (2026-07-20): the actual `ManagedVm` path now reserves a protected per-sandbox transaction with the authenticated owner and create-new-only semantics, so an ID collision cannot replace existing cleanup evidence. The platform containment hook durably records QEMU intent immediately before suspended child creation; after exact authoritative-Job assignment it queries PID and creation time through the still-open process handle, atomically commits that proof, and only then resumes the primary thread. Clean VM stop plus protected instance removal checkpoints `cleaning`, clears the proven record, and removes the ledger; any setup/teardown ambiguity retains it. Startup now treats every valid retained ledger as an outstanding obligation and stays health-only rather than admitting work before the pending Windows exact-proof cleaner has converged it. Service compilation and the prior full serial run pass (125 tests, one helper ignored), both new create-new/terminal transaction regressions pass exactly, and the platform suite passes 175 tests with two hardware/helper cases ignored. This supersedes the older “production QEMU intent/commit ledger integration pending” wording below. CON-01/SEC-02 remain `Windows implementation in progress`: startup PID/creation-time/image/Job re-query and termination, crash/reboot convergence, completion-port/helper/nested-Job proof, integration of every non-QEMU resource, and real LocalSystem/exact-artifact evidence are still pending.
- SEC-02 exact QEMU startup-recovery tranche (2026-07-20): after adjacent-bundle verification, startup now feeds every unique valid ledger through the durable recovery executor before deciding admissions. A committed QEMU record opens one retained process handle with query/synchronize/terminate rights, treats an absent PID as idempotent success, and requires both the recorded creation time and exact verified-bundle image before termination; PID reuse or image mismatch quarantines without mutation. Termination waits at most five seconds for the exact handle, while access, query, terminate, or wait ambiguity preserves retry-required health-only state. Intent-only records select no PID because their child never resumed before commit and the authoritative kill-on-close Job cannot survive the old service process. All non-QEMU records remain retry-required. Compilation and strict scoped Clippy pass. The non-Job service run passed 126 tests; its sole SCM heartbeat timing miss passed an immediate exact rerun. Exact non-destructive tests prove valid-document convergence/admission reopening and intent-only no-process selection; no termination branch was executed. This supersedes the startup-QEMU-cleaner pending clause immediately above. SEC-02 remains `Windows implementation in progress`: real service-crash/reboot/forced-stop evidence, protected-ledger tamper/disk-full/power-cut tests, and exact adapters for every staging/account/share/ACE/WFP/port/relay resource are pending.
- CON-01 completion-monitor tranche (2026-07-20): every production QEMU Job now creates and associates a private I/O completion port before any process assignment. The existing VM worker drains a maximum 64 notifications each 100 ms tick, accepts only the closed new-process, normal/abnormal-exit, active-process-zero, and configured resource-limit message set with the exact private completion key and nonzero 32-bit PIDs, and tracks all QEMU/helper membership. Duplicate admissions, unknown exits, malformed keys/PIDs/messages, limit events, excessive batches, completion-port errors, or active-process-zero while the VM is running fail containment, terminate the Job, and preserve the ledger for recovery. Windows compilation, 127 serial non-Job service tests, strict scoped Clippy, and two pure decoder/tracker tests pass; no Job object or delivery/termination path was executed in this tranche. CON-01 remains `Windows implementation in progress`: approved real completion ordering/helper-tree/resource-limit delivery, nested enterprise Jobs, SCM/LocalSystem deadline/forced-stop/crash/reboot/WHPX behavior, and exact-artifact evidence remain pending.
- SEC-02 protected-instance transaction tranche (2026-07-20): the per-sandbox ledger is now reserved immediately after quota admission and before the sandbox-specific directory or rootfs exists. Preparation persists a staging-root intent before directory creation, commits its exact volume/file identity before rootfs copy/resize, and passes the same transaction into the authoritative QEMU Job. Preparation rollback and normal VM teardown re-query the directory and require one exact committed path/identity proof before recursive removal and ledger completion. The legacy start-error and panic fallback deletions were removed; ambiguity retains the tree and ledger. Compilation, 129 serial non-Job service tests (four Job tests filtered), strict scoped Clippy, and the exact standard-token filesystem/transaction tests pass. SEC-02 remains `Windows implementation in progress`: crash-time handle-relative staging-tree deletion, rootfs/file proof as needed, retained-tree reboot/tamper/TOCTOU evidence, and remaining account/share/ACE/WFP/port/relay adapters are pending.
- SEC-02 committed staging-recovery tranche (2026-07-20): startup now gives the Windows exact-proof cleaner the validated ledger document, ledger filename ID, verified bundle root, and protected users root. A committed staging record is eligible only at the authenticated owner's deterministic 64-hex directory and exact 32-hex ledger sandbox ID. Recovery opens the users root no-follow, traverses every component with root-relative `NtCreateFile`, rejects intermediate/final reparses, verifies the retained final handle's volume/file identity, enumerates at most 100,000 entries and 256 levels through pinned child handles, never follows reparse children, and requests delete disposition on those exact handles. Absence converges idempotently, including an intent-only record whose owner-bound path was never created; an existing intent-only directory remains untouched and health-only. Owner/filename/path or file-ID contradiction quarantines without mutation, and open/enumeration/sharing/delete ambiguity retains retry-required health-only state. All 133 serial non-Job service tests (four Job tests filtered), five exact standard-token cleaner tests, compilation, and strict scoped Clippy pass. This supersedes the crash-time handle-relative staging-tree pending clause immediately above. SEC-02 remains `Windows implementation in progress`: an existing intent-only create-before-identity directory needs administrator repair, rootfs/file proof as needed, approved real crash/reboot/reparse/sharing/tamper/TOCTOU evidence is pending, and account/share/ACE/WFP/port/relay adapters remain incomplete.
- PAR-01 contract tranche (2026-07-19): `contracts/seawork-parity-v1.json` now records the pinned SeaWork `0ae88c6d338ffb10d765296625ea38b3b3991f64` / LocalSandbox `0.4.6` reachability inventory, safe service mappings, fixed capacity and timeout decisions, explicit backlog blockers, and four external sign-off roles. Five deterministic helper-versus-service workload fixtures cover workspace/output shell behavior, skills/files, public authenticated networking, both host-connectivity directions, and lost-start recovery. `cargo run -p xtask -- verify-seawork-parity --seawork-repo ~/code/seawork` validates the contract/fixture schema and rechecks eight positive/negative assertions against the pinned source. PAR-01 remains `macOS implementation in progress; Windows comparison and owner sign-off pending` because the dual-backend Windows runner and executed evidence are not yet complete.
- LIF-01 start/replay tranche (2026-07-19): protocol 1.2 introduced bounded `instanceId`/`from` compatibility hints without restoring `dataDir`, bundle paths, QEMU paths, owner identity, or cleanup manifests; protocol 1.4 now gives `instanceId` fail-closed one-shot replay semantics. A committed replay on the owning connection returns the same service-generated opaque handle, concurrent duplicate creation is rejected, failures before commit release the key for a deliberate retry, and active records are never capacity-evicted. Disconnect cleanup turns the key into a bounded 10-minute tombstone, so reconnect receives stable non-retryable `START_RESULT_EXPIRED` instead of adopting the old sandbox or creating a second VM. The cache is capped at 4,096 records and scoped to the authenticated user/logon identity; caller text never selects a path or handle. Any non-empty `from` still fails before reservation or filesystem effects with non-retryable `CHECKPOINT_UNSUPPORTED`. All 24 protocol and 28 macOS host-neutral service tests pass. LIF-01 remains `macOS implementation complete; real lost-response, WHPX/QEMU lifecycle, reboot/update/rollback, and downstream adapter evidence pending`.
- API-01 shell/error-parity tranche (2026-07-19): service-backed Node `exec`, cancellable `beginExec`, and `spawn` now accept the reachable SeaWork `shell` option and share the direct binding's exact normalization: string commands become `[shell || "sh", "-c", command]`, while argv commands ignore the shell option. This uses the existing bounded argv wire contract and therefore remains compatible with older negotiated service minors. Service error reasons now retain their stable code prefix without exposing correlation IDs, allowing downstream recovery to distinguish `START_RESULT_EXPIRED` and other safe categories. Four binding unit tests, all 10 API/package/startup tests, generated declarations, and TypeScript checking pass.
- CAN-01 commit-aware cancellation tranche (2026-07-19): protocol 1.5 makes request cancellation/deadline and synchronous filesystem commit a single atomic race. `Mkdir`, `Remove`, `Rename`, `Copy`, and `Chmod` enter commit immediately before their opaque guest call; `WriteFile` remains cancellable through sibling staging and enters commit immediately before atomic rename. Once committing, Cancel returns stable non-retryable `CANCELLATION_TOO_LATE` while the original request reports its actual result; negotiated 1.4 clients receive known `REQUEST_NOT_ACTIVE`. The dispatcher now waits for the worker, including pre-commit sibling cleanup, before publishing cancellation or deadline. Typed deadline reasons no longer collapse to cancellation. All 26 protocol, 1 client, 32 macOS host-neutral service, 5 Node binding, and 20 xtask tests pass; strict protocol/client Clippy and the pinned parity verifier pass. The Windows service cross-check stops in AWS-LC/ring before service Rust because this Mac lacks `stdlib.h`, `assert.h`, and `windows.h`. CAN-01 remains `macOS implementation in progress; real guest-call, cleanup-failure, connection-loss, caller-exit, and SCM drain verification pending`; recursive guest mkdir/copy/remove failures are reported but not claimed rollback-atomic. The exact model is in `docs/filesystem-cancellation.md`.
- SEC-01 endpoint-pinning/ACL tranche (2026-07-20): before sending Hello, the Windows client requires the SCM registration's service SID type to be exactly `SERVICE_SID_TYPE_UNRESTRICTED`, opens and retains the RUNNING service process handle, and opens the exact configured packaged executable without write/delete sharing. The executable must be a single-link regular non-reparse file under the Windows `ProgramFiles` known folder. The client opens and retains the `SeaWork` through `bin` ancestor directories without write/delete sharing, rejects reparse ancestors, and requires every normalized handle final path to match the parsed package chain. Every held executable/directory descriptor must be owned by SYSTEM, Administrators, or TrustedInstaller and contain no applicable write/delete/ACL/owner-control allow ACE for any other principal; null, malformed, unreadable, unfamiliar compound-write, or untrusted descriptors fail closed. The held executable then passes no-UI, cache-only `WinVerifyTrust`, and its embedded primary signer's SHA-256 certificate thumbprint must equal a compiled allowlist of the required current `SEAWORK_PUBLISHER_SHA256` plus at most one distinct optional `SEAWORK_PUBLISHER_SHA256_PREVIOUS`; malformed, duplicate, empty-current, and larger policies fail the client build, while service signing and the bundle manifest continue to name only the current identity. One-publisher and overlap release checks pass and a duplicate policy fails closed without inspecting configured signing material. The client requires configured/running executable identity equality and re-queries complete SCM own-process/LocalSystem/SID/command configuration, pipe PID, RUNNING SCM PID, and process lifetime. The server queries every authenticated client process image twice around opening the reported executable without write/delete sharing and with final-component reparse traversal disabled, requires a single-link regular file whose handle-resolved final path equals the process path, and retains that image handle with the process/pipe-token identities through Authenticode/publisher authorization and the live connection. A standard-token current-process test exercises that real Windows pin. Endpoint constants are centralized in the protocol crate: the non-default `development-service` feature propagates through service/client/Node and selects only `LocalSandboxSeaWorkDev`, `\\.\pipe\LocalSandbox.SeaWork.Dev.v1`, and `%ProgramData%\LocalSandbox\SeaWorkDev`, while default production identities remain unchanged. The deterministic packager accepts an explicit production/development profile, emits distinct development artifact names and the matching generated contract, and rejects profile-mismatched service PEs during both staging and archival while retaining the production publisher, catalog, manifest, and ACL gates. Default, development, and release protocol runs pass 28 tests each; the development client passes all 21 tests, its Node binding compiles, the production release service PE contains the production identities and no development identity strings, and all 25 xtask tests pass. The feature does not relax signing or bundle policy. The latest serial service run passes 122 tests with one helper intentionally ignored; all 21 client tests, client all-target compilation, strict client Clippy, and strict service Clippy with only the four known unrelated findings allowed pass. SEC-01 remains `Windows implementation in progress; installed package owner/DACL evidence, real publisher-rotation rollout/removal, elevated development installation and any deliberate unsigned-client tooling, and real signed-artifact SCM/squatter/replacement/PID-token race, multi-user, low-integrity, AppContainer, remote-pipe, and wrong-signer evidence pending`.
- TST-01 authenticated dispatch saturation tranche (2026-07-20): deterministic in-memory and uniquely named real Windows kernel-pipe tests fill the exact 64-frame authenticated dispatch queue while two additional frames remain upstream, then prove ordered lossless recovery, writer unblocking, and restoration of all queue capacity before EOF. The real pipe uses a two-header kernel buffer and exists only for the standard-token test lifetime. A version-valid invalid-magic frame is delivered as one error, terminates the owned reader, and prevents a following valid frame from reaching dispatch. Four authenticated connections each hold the exact 16-request per-connection limit, exhausting the shared 64-request semaphore; a fifth is rejected with `QUOTA_EXCEEDED`, then acquires exactly one permit released by cancellation, and all five servers drain cleanly. The test-only delay is serialized and reset by an RAII guard. The full serial service run passes 122 tests with one child helper intentionally ignored. TST-01 remains `Windows implementation in progress; installed endpoint multi-client CPU/memory/latency behavior and broader transport fault injection pending`.
- SEC-02 protected-ledger envelope tranche (2026-07-19): startup now treats the protected ledger as a closed, bounded directory instead of ignoring non-JSON state. Exact opaque-ID filenames, regular-file type, bounded enumeration/read/aggregate size, strict schema/proof shapes, unique document ownership IDs, and unique resource identities are required. Unknown files, interrupted temporaries, symlinks, malformed documents, forged ownership markers, and ambiguous duplicates move unread into bounded quarantine and close admissions; excessive or unmovable state remains unproven and health-only without deletion. Atomic persistence now serializes writers and uses random `create_new` siblings, file flush, replace, parent flush where supported, per-attempt cleanup, and concurrency tests. A host-neutral recovery executor durably enters `Cleaning`, removes in reverse dependency order only through an exact-proof adapter, checkpoints each removed/absent record, preserves retryable failures, quarantines identity mismatches, and deletes the ledger only after every proof. Fault injection covers every cleanup boundary and the crash window after external removal but before checkpoint. All 46 macOS host-neutral service tests and the pinned parity verifier pass; Clippy reports only the existing non-Windows cfg/dead-code and config-test findings. SEC-02 remains `macOS implementation in progress; Windows external-object proof adapters, crash/reboot convergence, and resource-complete ledger integration pending`. See `docs/protected-ledger-reconciliation.md`.
- NET-01 proxy-policy tranche (2026-07-19): default outbound policy now allows only globally routable IPv4/IPv6 destinations and rejects loopback, RFC1918/ULA, link-local, CGNAT, multicast, unspecified, reserved/documentation, metadata-adjacent, IPv4-mapped, and WPAD destinations before connection. Host-resolved A and AAAA answers and the rebinding cache retain only public addresses; mixed answers are filtered fail closed, each family refreshes and expires independently, and the dual-stack smoltcp/initramfs path carries public IPv6 TCP traffic over an isolated `fd00::/64` guest link. Exact and `*.example.com` scopes are validated and IDNA-normalized with case/trailing-dot handling, and wildcard scopes exclude their apex. Intercepted HTTPS and allowlisted plaintext HTTP/1.1 reauthorize every request Host against the normalized connection domain and port; cross-host reuse is rejected, fragmented initial headers are bounded, and a redirect's new connection recomputes all scopes. Default non-TLS TCP now passes the same public-destination check before any direct or proxy connection, closing a pre-existing private-destination bypass. An optional installer-protected upstream proxy uses a bounded HTTP CONNECT handshake to the already-approved IPv4 or bracketed IPv6 destination; its explicit authorization is redacted, zeroized, and scoped only to the configured endpoint. There is no WPAD, environment/system proxy discovery, or default Windows credential path. All 77 `lsb-proxy` tests pass. Strict crate Clippy reaches only four pre-existing findings in `proxy.rs`, `stack.rs`, and `lib.rs`; the modified policy/config/DNS/HTTP paths add no new finding. The Windows proxy cross-check cannot compile native `ring`/AWS-LC dependencies on this Mac without the Windows SDK/MSVC headers. NET-01 remains `macOS implementation complete; real proxy/redirect integration evidence and managed Session 0 evidence pending`.
- NET-01 service-API tranche (2026-07-19): protocol 1.3 defines bounded, strict allowlist, host-scoped secret, HTTPS interception, scoped request-header, and host-scope contracts behind three explicit negotiated feature bits. Secret and injected-header wire values redact `Debug` output, zeroize on drop, and travel through zeroizing client/server JSON buffers. The Rust client advertises only known bits, rejects unnegotiated network options locally, and rejects unknown selected bits. The service intersects feature offers and rejects requests lacking negotiated bits; feature advertisement remained gated until the activation tranche below. `SeaWorkStartOptions.network` maps the existing Node `NetworkConfig` shape while rejecting `exposeHost` until NET-02. The 24 protocol tests, strict Windows protocol/client Clippy, Windows service cross-check, macOS Node compile, generated declarations, five API/package/startup tests, and TypeScript checking pass.
- NET-01 service-activation tranche (2026-07-19): new Rust and Node starts request default public egress, and the service now advertises the negotiated egress, secret, and HTTPS-interception features. Before admissions or filesystem side effects, it converts and validates the wire policy, intersects it with installer-protected `egress_allow`, and creates a proxy link attached to the service-owned VM. The proxy lives through VM teardown, opaque secret placeholders are merged only into guest exec/spawn environments, HTTPS interception installs the ephemeral CA in the guest, and secret/header values zeroize when dropped. On Windows, upstream TLS reads only the LocalMachine ROOT/CA stores and fails startup closed when they are unavailable. An optional bounded PEM product CA is read once from the fixed installer-protected `config/product-ca.pem` path, strictly parsed before admissions, redacted from config diagnostics, and added only to upstream proxy TLS trust. Strict protected `upstream_proxy` service configuration carries one explicit endpoint and optional authorization value into every VM proxy without exposing either through caller APIs. The 77 proxy tests, 22 protocol tests, all 25 macOS host-neutral service tests, strict Windows protocol/client Clippy, macOS Node check, and `lsb-vm` check pass. The full Windows service cross-check stops in AWS-LC before compiling service Rust because this Mac lacks `windows.h`; Windows CI is required to type-check and exercise the final VM attachment path. NET-01 remains `macOS implementation complete; real proxy/redirect integration evidence and managed Session 0 evidence pending`.
- OBS-01 diagnostics tranche (2026-07-19): lifecycle logging now uses a typed append-only 16-event catalog shared by the Rust service and `.mc` message table; numeric drift is tested. Protected JSON records carry schema/service/bundle/protocol/ledger versions and only bounded closed-alphabet phase, stable-code, correlation, hashed-identity, opaque-resource, duration, and Win32 fields. The 10 x 10 MiB writer serializes appends, rotates before a record would cross its bound, removes only the oldest regular generation, and rejects symlink/non-file tampering. SCM startup, RUNNING, STOP/preshutdown, fatal pipe/control exit, bundle, quarantine, capability, drain, and cleanup paths use the typed catalog instead of dynamic pseudo-codes. Windows builds register an Application Event Log source handle and report the same IDs; the release workflow resolves explicit Windows SDK `mc.exe`/`rc.exe` paths and links the generated `.res` without PATH discovery. Pre- and post-sign Windows gates load the exact PE as a data/image resource, prove IDs 1-16 and the absence of unreviewed ID 17, and publish the signed binary's SHA-256 and verified IDs as machine-readable release evidence. Six macOS host-neutral diagnostics tests cover catalog drift, schema/redaction validation, bounded rotation, concurrent complete records, and non-file/symlink tamper. All 52 service tests, four release-packaging tests, workflow YAML/PowerShell parsing, and the pinned parity verifier pass; Clippy adds no diagnostics finding. The Windows cross-check again stops in AWS-LC/ring before service Rust because this Mac lacks `stdlib.h`, `assert.h`, and `windows.h`. OBS-01 remains `macOS implementation in progress; Windows SDK compile/link and PE-resource gate execution, installed EventMessageFile registration, Event Log inspection, disk-full/update/rollback/uninstall integration, and tamper runtime evidence pending`. See `docs/service-diagnostics.md`.
- MNT-01 staged-sync authorization/copy tranche (2026-07-20): fixed mount bounds have one protocol source of truth at 100,000 entries, 20 GiB/tree, 4 GiB/file, 256 components, 32,767 UTF-16 code units, 100 queued changes, and a 16 MiB fast-copy threshold. The dedicated Windows caller-token path worker runs an explicit handle-DACL `AccessCheck` for every pinned ancestor, the root, every authorized tree entry, and every source entry reopened for staging; the walk summary records the number of checks. After one absolute local-volume bootstrap, every descendant ancestor/root/tree/copy source is opened as one validated `NtCreateFile` component relative to a pinned parent without delete sharing and with open-reparse-point semantics; rights-specific reopens require stable volume/file identity and metadata. Staging enforces entry, per-file, and aggregate-byte bounds during actual I/O, limits each read to the authorized length plus one byte, and rejects a source whose held handle changes identity, length, or last-write time before copy completion. Protected-profile policy read-only enumerates and bounds the machine `ProfileList`, expands and validates local `ProfileImagePath` values, and denies every discovered profile except the authenticated caller. Every system, service, ProfileList, profiles-directory, and caller-profile root is now pinned without delete sharing; its 64-bit volume serial and 128-bit file ID are compared against every mount-chain handle before tree inspection. Profile-derived aliases of the caller are excluded canonically, but an identity collision never removes a system/service root; unreadable, missing, reparse, or non-directory protected roots fail authorization. Real current-token DACL, component-chain legacy/full identity, recursive walk/staging, current-machine ProfileList, and pure alias-policy regressions pass; the full serial service run passes 121 tests with one child-entry helper intentionally ignored. MNT-01 remains `Windows implementation in progress; active monitoring, caller-token writeback, retained-stage recovery, periodic/final flush, admin live SMB/service activation, and real NTFS/ReFS/GPO/alternate-name/path-swap/adversarial/performance evidence pending`; protocol mount requests still return `MOUNT_UNAVAILABLE`. See `docs/staged-mount-sync.md`.
- MNT-01 caller-token export-commit tranche (2026-07-21): the existing source-only writeback primitive now caps an export at the authorized protected-source length plus one byte, requires the same open source handle to retain exact length and last-write time through the copy, flushes a random create-new sibling, limits best-effort failure cleanup to that sibling, and uses write-through atomic replacement instead of deleting the caller destination first. A standard-token regression proves successful replacement and proves an over-length source publishes neither a destination nor a temporary. All 134 serial non-Job service tests pass with four Job tests filtered, and strict scoped Clippy passes. MNT-01 remains `Windows implementation in progress`: writeback destinations are not yet capability-bound or traversed handle-relatively, active/periodic/final synchronization and conflict publication are not wired, SMB/account/ACE/share lifecycle is absent, and mount requests remain fail-closed with `MOUNT_UNAVAILABLE` pending approved real NTFS/ReFS/GPO/race/performance evidence.
- MNT-01 capability-bound writeback tranche (2026-07-21): `PathWorker::export_file` now accepts an `AuthorizedMountRoot` and relative destination instead of a naked caller destination, rejects read-only capabilities, and duplicates the retained root into the impersonated worker command. Each existing or missing parent is opened or created with `NtCreateFile` relative to the preceding pin, with open-reparse-point semantics and no delete sharing. The random create-new sibling is flushed and its exact handle is atomically renamed relative to the pinned parent with `FileRenameInformationEx`; no destination path is reopened. Prefix, traversal, separator, NUL, and ADS components fail closed, non-overwrite collision preserves the existing file, and failure cleanup selects only the exact temporary handle. A standard-token regression covers replacement, collision, missing-parent creation, bounded-source failure, namespace rejection, and recursive temporary cleanup. The exact regression, strict scoped Clippy, and the serial non-Job service suite pass (134 tests, four Job tests filtered); one initial parallel run's known SCM heartbeat timing miss passed both exact and serial reruns. Mounts remain `MOUNT_UNAVAILABLE`: protected-source capability/ledger binding, ordered periodic/final reconciliation, conflict publication, stage lifecycle, SMB/account/ACE/share integration, and approved adversarial NTFS/ReFS evidence remain incomplete.
- MNT-01 staged-reconciliation controller tranche (2026-07-21): every prepared `StagedMount` now owns its baseline through a terminal-state controller with one-second dirty, 30-second idle, and mandatory 30-second final-flush scheduling. It validates all three bounded snapshots, preflights every conflict before exposing any I/O, orders deepest-first deletions ahead of parent-first directories/files, advances the baseline only after explicit whole-plan completion, rejects foreign/stale plans and backwards clocks, preserves notifications that arrive during an in-flight cycle, and requires another final catch-up cycle when newer dirt exists. Conflict, reported I/O ambiguity, or final deadline expiry fails terminally rather than reporting teardown success. Exact pure tests cover dependency ordering, conflict preflight, foreign-plan rejection, periodic/dirty/final triggers, in-flight generation retention, final catch-up, terminal I/O failure, and deadline expiry; the full serial non-Job service suite passes 136 tests with four Job tests filtered, and strict scoped Clippy passes. This is non-activating orchestration only: fresh capability-bound host/stage snapshots, operation execution, conflict publication/recovery journaling, VM stop/cleanup integration, and Windows timing/adversarial evidence remain incomplete, so mounts stay `MOUNT_UNAVAILABLE`.
- MNT-01 protected-source capability tranche (2026-07-21): after initial staging, `StagedMount::prepare` now opens the exact staging directory with no-follow and no-delete-sharing semantics, rejects reparse/EFS/offline/cloud-recall attributes, retains it as a `ProtectedStagingRoot`, and commits that handle's volume/file identity to the protected ledger before returning. `PathWorker::export_file` accepts this unforgeable capability plus a bounded relative source instead of a naked protected path. Before caller impersonation, the worker traverses every source component relative to the retained staging handle without delete sharing and requires a regular non-reparse final file; destination traversal and publication remain rooted at the retained read-write caller capability under impersonation. Standard-token exact tests prove the committed identity matches the pin, directory deletion is denied while pinned, both source/destination namespace escapes fail, and successful/failed atomic export behavior remains bounded. The serial non-Job service suite passes 137 tests with four Job tests filtered, and strict scoped Clippy passes. Mounts remain `MOUNT_UNAVAILABLE`: the reconciliation controller still lacks fresh capability-bound snapshot/operation execution, conflict publication/recovery journaling, VM stop/cleanup integration, SMB lifecycle, and approved adversarial Windows evidence.
- MNT-01 pinned fresh-snapshot tranche (2026-07-21): the filesystem worker can now produce fresh snapshots from duplicated `AuthorizedMountRoot` and `ProtectedStagingRoot` handles without reopening either root by path. It enumerates each exact directory handle with `GetFileInformationByHandleEx`, validates every returned component, and opens each child with `NtCreateFile` relative to its parent without delete sharing or reparse traversal. Host snapshots run under the retained caller token and repeat explicit DACL `AccessCheck`; protected-stage snapshots run under the service token. Both modes reject unsafe attributes, multi-link and repeated identities, enforce path/entry/file/tree bounds, hash only held file handles, and re-query identity, size, and last-write time after every read. Initial staging now derives the controller baseline from the newly pinned protected root. A standard-token regression mutates, removes, and adds nested entries and proves fresh host and protected snapshots agree; the serial non-Job service suite passes 138 tests with four Job tests filtered, and strict scoped Clippy passes. Mounts remain `MOUNT_UNAVAILABLE`: the controller does not yet request snapshots per cycle or execute ordered imports/exports, and conflict publication/recovery journaling, VM stop/cleanup integration, SMB lifecycle, and approved adversarial Windows evidence remain incomplete.
- MNT-01 bound cycle-observation tranche (2026-07-21): `StagedMount` now privately owns its reconciliation controller and records the authorized host root's volume/file identity, caller SID, and access mode at preparation. Only when a dirty, periodic, or final cycle is due does it request fresh host and protected-stage snapshots through the filesystem worker; a different host capability or either observation failure makes the controller terminal before any plan is exposed. A pure regression covers the new terminal pre-plan failure transition. The serial non-Job service suite remains green at 138 tests with four Job tests filtered, and strict scoped Clippy passes. Mounts remain `MOUNT_UNAVAILABLE`: ordered import/export mutation execution, post-operation snapshot proof, conflict publication/recovery journaling, VM stop/cleanup integration, SMB lifecycle, and approved adversarial Windows evidence remain incomplete.
- MNT-01 capability-bound host-import tranche (2026-07-21): the filesystem worker now accepts only an `ImportHost` operation with retained authorized-host and protected-stage capabilities. It opens and DACL-checks the observed source beneath the host pin under caller impersonation, rejects unsafe attributes, multiple links, type/length drift, and disappearance/reappearance, then reverts before mutating the protected stage. Files copy from the retained source into a random create-new stage sibling, must match the planned length/content hash and stable handle identity/metadata, flush, and atomically replace by exact-handle rename. Directory creation, file-to-directory replacement, and deepest-first deletion use relative opens and exact-handle disposition. A standard-token regression executes all shapes and proves normalized host/stage snapshots agree with no temporary left behind. An initial full run exposed that read-only recovery identity probes had inherited new staging mutation rights; those APIs were split, both exact cleanup regressions passed, and the final serial non-Job suite passes 139 tests with four Job tests filtered; strict scoped Clippy passes. Mounts remain `MOUNT_UNAVAILABLE`: the controller does not yet dispatch plans or prove post-operation snapshots, guest-to-host directory/deletion mutations are absent, and conflict publication/recovery journaling, VM stop/cleanup integration, SMB lifecycle, and approved adversarial Windows evidence remain incomplete.
- MNT-01 symmetric execution/proof tranche (2026-07-21): `PathWorker::export_operation` now accepts only `ExportGuest` with a retained read-write host capability and protected-stage capability. It proves planned absence/directory/file state beneath the stage pin before impersonation; under the caller token it applies directory creation, type replacement, and deepest-first exact-handle deletion, while planned files are length/content-hash checked during bounded atomic publication. A wrong planned hash publishes no destination and cleans its exact temporary. `StagedMount::execute_plan` dispatches every preordered import/export, obtains new capability-bound snapshots of both sides, and calls a verified completion path that advances the baseline only if both trees semantically equal the plan's expected snapshot; mutation, snapshot, or outcome drift clears the pending cycle and makes the controller terminal. Exact standard-token and pure regressions cover the symmetric namespace operations, wrong-hash failure, verified success, and post-operation drift; the serial non-Job service suite passes 140 tests with four Job tests filtered, and strict scoped Clippy passes. Mounts remain `MOUNT_UNAVAILABLE`: conflict artifact publication/recovery journaling, watcher/periodic/final runtime scheduling, retained-stage user export, VM stop/cleanup and SMB lifecycle integration, and approved adversarial Windows evidence remain incomplete.
- MNT-01 conflict-retention/control-surface tranche (2026-07-21): divergent relative paths are now retained in the terminal reconciliation controller and exposed through `StagedMount` alongside its still-pinned `ProtectedStagingRoot`, rather than existing only in the returned `MOUNT_CONFLICT` error. The mount keeps the controller private but forwards bounded change notification, final-flush initiation, state, and conflict inspection needed by a runtime driver. The existing controller regression now proves exact retained conflict paths; the focused test passes. Mounts remain `MOUNT_UNAVAILABLE`: conflict artifact publication and durable recovery journaling, a real Windows host monitor, guest-event bridge, periodic/final runtime scheduler, retained-stage user export, VM stop/cleanup and SMB lifecycle integration, and approved adversarial Windows evidence remain incomplete.
- WIN-01/TST evidence tranche (2026-07-20): `cargo run -p xtask -- verify-windows-evidence --manifest <path>` validates the prescribed `artifacts/windows-evidence/<git-sha>/<artifact-sha256>/` layout, a closed schema with `win01`, `security`, and `full` profiles, 18 named release checks, commit/artifact digest binding, safe environment/version fields, hashed runner/policy identity, bounded durations/counts, canonical case-unique relative paths, regular-file size/SHA-256 closure, explicit redaction declarations, and per-check evidence references. `--require-complete` rejects a missing or non-passing required check; `--artifact` rehashes and size-checks the exact candidate; `--require-profile` prevents a complete subset from satisfying a broader gate. Incomplete handoffs retain `failed`, `blocked`, or `not_run` with stable codes. `scripts/assemble-windows-evidence.ps1` refuses overwrite, hashes the exact artifact, rejects evidence lacking a `.redacted` name, and rejects common raw account SIDs, absolute/UNC paths, credential/private-key/certificate identifiers, and machine/user fields before copying. All 24 xtask tests, strict xtask Clippy, Rust formatting, diff checks, and PowerShell parsing pass; four validator tests cover accepted complete security evidence, blocked handoff versus release completeness, post-manifest tamper, and full-profile exact-artifact size/hash binding. WIN-01/TST-01/TST-02 remain `harness implementation in progress; privileged SCM/LocalSystem, multi-user, Job, SMB, WFP, crash/reboot, Event Log, installer/lifecycle, and managed-fleet executions pending`. See `docs/windows-acceptance-evidence.md`.
- CI-01 exact-artifact Windows release gate tranche (2026-07-20): the signed-service release now stops before publication on the protected `seawork-service-production` environment and a runner labeled `self-hosted`, `Windows`, `X64`, and `seawork-service`. It hashes the sole payload ZIP, derives only `<SEAWORK_WINDOWS_EVIDENCE_ROOT>/<GITHUB_SHA>/<artifact-sha256>/manifest.json`, refuses an escaping/missing or pre-existing workspace destination, copies the closed redacted evidence tree, and invokes the Rust validator with the downloaded artifact, required `full` profile, and complete-check policy. The validated evidence copy is retained for 90 days, and `publish-seawork-service` depends on this job. Workflow YAML and its PowerShell gate parse locally. CI-01 remains `source gate implemented; protected environment/runner/root provisioning, exact signed-artifact full Windows execution, reviewer approval, and live release enforcement pending`.
- CON-01 authoritative-Job tranche (2026-07-20): the production Windows platform QEMU supervisor launches with `CREATE_SUSPENDED | CREATE_NO_WINDOW`, opens/revalidates the sole initial thread, and resumes only after containment. Production `ManagedVm` now creates one service-owned Job before its worker thread, applies the existing 8-process and guest-memory-plus-2-GiB limits, passes that opaque boundary through `lsb-vm`/`lsb-platform`, and prevents the platform from creating a second Job. A normal stop uses the same Job; the bounded deadline covers STOP queueing, cleanup reply, and actual worker exit. At the deadline it terminates the Job, allows five seconds for cleanup, and aborts the service rather than report `STOPPED` while detaching a stuck live thread. Unexpected drop best-effort terminates the Job. Missing/multiple threads, owner revalidation failure, assignment/containment proof failure, or an unexpected suspend count terminate and reap the child fail-closed. Explicitly approved current-token runs passed all 16 QEMU supervisor tests, including external-Job child-entry containment and grandchild kill-on-close, plus the direct service `SandboxJob` suspended-child/termination proof; the final serial service run passes 116 tests with one child-entry helper intentionally ignored. An earlier parallel run had one unrelated SCM-heartbeat timing miss that passed exact and serial reruns. Platform/service/VM compilation passes, service strict Clippy reports no findings in the touched service files, and the touched QEMU process module has no strict-Clippy findings. CON-01 remains `in progress`: production QEMU intent/commit ledger integration, every helper, nested enterprise Jobs, and real SCM/LocalSystem deadline, crash, forced-stop, reboot, WHPX, and exact-artifact evidence remain pending.

- Baseline branch: `feat/lsb-win-service`
- Investigated commit: `c9e447cec349723f6e70ee3b78dd429af171e879`
- LocalSandbox version: `0.4.6`
- Last verified implementation commit: `4f0fafa` (`fix(service): gate admissions on WHPX`)
- Phase 0 verification: schema test, Windows compile, isolated Clippy, and PowerShell parse pass
- Phase 1 verification: release build passes; 10 protocol and 7 service tests pass; isolated Clippy passes
- Phase 2 verification: release client/service build passes; 30 combined protocol/client/service tests pass; isolated Clippy passes
- Phase 3 verification: 27 service tests pass and isolated warning-clean Clippy passes on Windows
- Phase 4 verification: 171 platform tests, 36 VM tests, and 33 service tests pass on Windows; 13 hardware/elevation tests are ignored as documented; isolated service Clippy passes with `-D warnings`
- Phase 5 RPC verification: 71 service, 14 Rust client, and 19 protocol tests pass. The production service binary and all client targets pass `cargo clippy --no-deps -D warnings`; Rust 1.96 service all-target strict Clippy reports one pre-existing test `field_reassign_with_default` finding, while dependency-wide strict Clippy reports three pre-existing `lsb-proto` `io_other_error`/`manual_is_multiple_of` findings.
- Phase 5 Node verification: the Windows release binding compiles; N-API generation and two consecutive deterministic loader/declaration patch passes succeed; 10 unprivileged declaration/package/startup tests pass against the loaded addon; TypeScript declarations compile. The built addon exports `connectSeaWorkService`, `SeaWorkService`, `SeaWorkSandbox`, `SeaWorkProcess`, `SeaWorkWatch`, and `SeaWorkExecOperation`. The wrapper shares one `Send + Sync` Rust service client without holding an outer mutex across RPC awaits, so independent calls retain protocol multiplexing. Strict `lsb-service-client` Clippy passes; binding-wide Clippy still reports only the documented pre-existing direct-SDK `needless_return`/derivable-default findings and `lsb-sdk` `CasNbd` warning, with no new compatibility-surface findings.
- Phase 6 verification: 17 xtask packager tests and 53 installed-bundle/service tests pass; strict affected Clippy and all release PowerShell parsers pass. A real x86-64 release PE was built with static CRT and its `dumpbin /DEPENDENTS` report passed with 11 Windows-system imports; the checker rejects the prior dynamic-CRT binary for `vcruntime140.dll`. Deterministic payload/symbol archives were opened by .NET and repeated builds were byte-identical in the release fixture. The local signing smoke test signed and cataloged a fixture with the available private test identity in explicit untrusted/no-timestamp mode only; it is not production trust evidence.
- Full audit verification (2026-07-18): `cargo test --workspace --locked --offline` passes across the Windows workspace, including 173 platform tests, 36 VM tests with 11 hardware tests ignored, 60 service tests, 19 protocol tests, 7 client tests, and 16 xtask tests. The Windows CI-equivalent Node gate passes 10 AVA API/package/startup tests and TypeScript checking. All service workflow YAML and four release/spike PowerShell scripts parse. The only workspace warnings are the pre-existing `lsb-sdk` `CasNbd` dead-code and test unused-import warnings.
- Current-HEAD regression (2026-07-18): `cargo test --workspace --exclude lsb-sdk --locked --offline` passes, including 171 platform tests with 2 ignored, 36 VM tests with 11 hardware tests ignored, 71 service tests, 14 Rust client tests, 19 service-protocol tests, and 17 xtask tests. The unexcluded workspace run is blocked only by the pre-existing `lsb-sdk::assets::noop_progress_entrypoint_preserves_result_and_filesystem_state` test attempting a real managed-QEMU download in this restricted clean shell (`os error 10013`); an exact isolated rerun fails identically, so this unrelated network/cache-dependent test is recorded rather than changed under the shipping-first rule.
- Deferred verification: the current shell is not elevated and has no prepared runtime assets, so SCM LocalSystem/WHPX/SMB execution was not run; details are in `docs/windows-service-feasibility.md`
- Phase 1 foundation: process bootstrap first restricts DLL resolution to System32 and explicitly added protected user directories, and accepts service dispatch only through the packaged `--service` mode; empty or extra-argument launches fail before SCM connection while exact JSON version/bundle tooling modes remain available. SCM startup advances monotonic checkpoints across protected path setup, config/reconciliation, bundle verification, runtime/context construction, and pipe binding. Full adjacent manifest/hash/contract verification and engine discovery run on a scoped worker before engine admission; while that potentially slow operation runs, `START_PENDING` advances every two seconds with a 120-second wait hint. Verification failure records `BUNDLE_INVALID`, closes engine admissions, and still binds the health-only pipe. Once `RUNNING`, SCM control delivery and the pipe task are supervised together; unexpected pipe completion/error/panic drains sessions and reports a nonzero service-specific `STOPPED` instead of leaving a dead endpoint marked running. Accepted STOP/preshutdown drains best effort, advances `STOP_PENDING` checkpoints every two seconds, aborts a stuck pipe task at the 30/60-second deadline, records cleanup failure/timeout, and reports successful `STOPPED` so failure actions do not restart during update/shutdown. Health pipe source and SDDL validation are complete.
- Phase 1 backlog: real elevated SCM install/STOP/preshutdown timing and Event Log message compilation require further integration or an elevated machine with Windows SDK `mc.exe`/`rc.exe`. Force-closing the real QEMU Job at the shutdown deadline remains the separate Phase 4 supervisor integration backlog.
- Phase 2 foundation: unauthenticated clients are capped at 8 globally and 2 per kernel PID, Hello is limited to five seconds, accepts use a monotonic 20/s burst-40 token bucket, and authenticated requests use a 100/s burst-200 per-connection bucket. Before sending Hello, the client binds the pipe server PID to the RUNNING own-process LocalSystem SCM service, requires the exact quoted versioned-bundle executable command with the sole `--service` argument, and rechecks PID/status. After authentication, an idle connection may wait indefinitely for the first frame byte; the remainder of the header and payload retain their five/ten-second deadlines. One owned reader task feeds an exact 64-frame bounded dispatch queue and is aborted on dispatcher exit. In-memory and real Windows named-pipe tests prove saturation with two upstream frames, ordered lossless recovery, writer unblocking, full capacity restoration, fail-closed reader termination after one malformed-frame error, and exact 4x16 global request exhaustion/rejection/single-slot reuse. The service holds and monitors the authenticated process handle; process exit cancels active requests and closes the owner session even if a pipe handle was inherited. Every client image must be Authenticode-valid below a configured protected root and match the protected signer allowlist.
- Phase 2 backlog: real installed owner/DACL and two-user/two-logon SCM tests, real server replacement-race execution, development-only elevated install tooling without a production trust bypass, installed endpoint multi-client/resource saturation and broader transport fault injection, installed publisher-rotation rollout/removal evidence, and remote/low-integrity/AppContainer/squatter adversarial execution. These are retained as hardening/integration backlog under the shipping-first rule; current endpoint identities remain OS-derived and normal admissions fail closed when client roots or publishers are empty.
- Phase 3 foundation: privileged mount roots are constructed only on a dedicated client-token filesystem thread; local fixed NTFS/ReFS, protected-root including bounded machine ProfileList roots, reparse/EFS/cloud, hard-link, entry, and byte policy fails closed; after the volume bootstrap, traversal/copy opens are handle-relative to pinned parents; protected roots are retained and compared to the full mount chain by 64-bit volume/128-bit file identity; explicit handle-DACL checks use the held caller token for ancestors, roots, and tree/copy entries; RO and RW both select staged-sync; staging/export IO crosses the token boundary through held files; protected intent/commit records, exact staging identity cleanup, service ownership markers, and deterministic conflict detection are implemented.
- Phase 3 backlog: wire the capability into `lsb-platform` SMB/VM lifecycle instead of its isolated legacy path API; complete active change monitoring and periodic propagation, caller-token RW writeback, handle-based post-share proof, exact external account/share/ACE reconciliation, and elevated alias/path-swap/adversarial fixtures. The initial service and Node service API intentionally expose no mount option; protocol mount requests return `MOUNT_UNAVAILABLE`. This backlog gates enabling mounts, not shipping the narrowed mount-free service, and no gap permits raw caller-tree sharing.
- Phase 4 foundation: trusted engine assets are bundle-confined; the service-selected QEMU path bypasses environment, managed-tool, and PATH discovery; a dedicated service VM thread owns the real `lsb-vm::Sandbox` with protected instance paths and no caller data directory, mounts, ports, checkpoints, or host exposure. Startup loads `winhvplatform.dll` only through the System32-only dynamic loader and queries `WHvCapabilityCodeHypervisorPresent`; absence, a failed query, or a reboot-pending platform reports `WHPX_UNAVAILABLE` and keeps admissions closed while health remains available. The built PE import table confirms no load-time `winhvplatform.dll` dependency. Session preparing/running slots reserve exact sandbox count, CPU, RAM, fixed 2 GiB global VM overhead, and logical disk before filesystem or boot side effects, freeze owner identity, propagate disconnect cancellation, and release failed starts. Global RAM is capped at protected config and 75% of physical memory, and disk admission checks actual protected-volume free space. The separate service launcher proves `CREATE_SUSPENDED` -> Job assignment -> resume and durable process intent/commit ordering. Egress rejects local/private rebinding and host ports remain disabled while WFP evidence is absent.
- Phase 4 backlog: make the real platform QEMU supervisor consume the service-owned external Job so a VM-thread stop timeout can force-close it without detaching the thread; connect staged mounts after Phase 3 SMB capability wiring; exercise Session 0 boot/exec/stop and cancellation on prepared assets. Combined platform Clippy currently has existing unrelated warnings, so modified service code was verified with isolated `--no-deps -D warnings`.
- Phase 5 lifecycle foundation: strict start/stop/close schemas exclude trusted runtime and identity fields; installed bundle discovery is fixed to the service-adjacent runtime/QEMU layout; missing assets or unavailable WHPX degrade health and close admissions. Start accepts 1-8 vCPU, 512-8192 MiB RAM, and 1024-32768 MiB disk, prepares a bounded rootfs below a stable authenticated-identity hash only after quota reservation, and removes preparation state on cancellation, failure, or task panic. Unsupported mounts/ports/network policy fail closed, stop is owner-bound, disconnect or owner-process exit cancels the VM, and exact instance cleanup runs after VM stop. The Rust client exposes typed lifecycle objects and preserves stable service error envelopes.
- Phase 5 process foundation: owner-bound unary `Exec` accepts only bounded argv-or-shell commands, normalized guest cwd, and bounded environment data. It runs on the managed VM command thread with a bounded queue/deadline and a combined 8 MiB stdout/stderr ceiling; the Rust client returns a typed result. `Spawn` reserves the 64/sandbox, 128/user, and 256/global process quotas before opening a guest session, returns owner-bound process/stdout/stderr handles, and bypasses the SDK's unbounded process channels. Guest frames are split into a 64-frame/under-4-MiB queue, output stalls kill only that process after 30 seconds, kill is idempotent until the ordered exit event retires the handle, and sandbox/session cleanup kills and releases every process.
- Phase 5 file foundation: owner-bound `Mkdir`, `ReadDir`, `Stat`, `Remove`, `Rename`, `Copy`, `Chmod`, and `Exists` validate canonical guest-absolute paths and run through the bounded managed VM command queue. The Rust client exposes typed directory/stat results. `ReadFile` and `WriteFile` transfer bytes only through binary stream frames, never JSON. `WriteFile` now writes a random sibling temporary guest file and renames on success, removing the temporary file if commit fails.
- Phase 5 watch foundation: `Watch` validates a canonical guest path, reserves the 64/sandbox, 128/user, and 512/global quotas before opening the guest session, and returns an owner-bound opaque handle. The service bypasses the SDK's unbounded channel with a 256-event queue that coalesces repeated paths and collapses saturation to `Overflow`; events share the strict server event sequence and bounded writer. `StopWatch` is tombstoned/idempotent, discards buffered events before acknowledgement, and closes the cloned guest control session; sandbox/session cleanup stops and releases every watch. The Rust client installs its route before waking the caller and coalesces a slow consumer to the latest event.
- Phase 5 Node foundation: the existing N-API package exports the planned `connectSeaWorkService(options)` entry point, `SeaWorkServiceClient`/`ServiceSandboxStartOptions`/`RemoteSandbox`/`RemoteProcess` declaration aliases, negotiated `getServiceInfo`, composite `healthCheck`, required-options `startSandbox`, and `close`, all backed only by `lsb-service-client`. The earlier `SeaWorkService.connect()`/`health()`/`start()` API remains as a compatibility alias. Connect timeout is bounded to 1-60000 ms and is one monotonic budget across transient pipe-unavailable/Hello-I/O retries with 25-250 ms exponential backoff; trust/protocol failures return immediately and the client never attempts SCM start. Generated start options contain only CPU, memory, and disk, and the API-shape test rejects trusted path/identity fields. Health, lifecycle, bounded exec, guest filesystem methods, credited spawned processes, managed watches, and cancellable exec operation handles are exposed without JavaScript pipe/security code. Service and sandbox wrappers share the multiplexed Rust client directly, so one long request does not serialize unrelated Node callers. `SeaWorkProcess` provides independently consumable bounded stdout/stderr chunks, async kill, and ordered exit; `SeaWorkWatch` provides the opaque ID, coalesced next-event reads, and async stop; `SeaWorkExecOperation` provides the full opaque request ID plus cancel/complete. The existing direct `Sandbox` API remains for non-service compatibility.
- Phase 5 stream foundation: protocol types define bounded cancel/window-update/event/close controls and sequenced binary stream payloads. `ReadFile` and `WriteFile` use random stream IDs, strict declared lengths and per-stream sequence checks, and binary `StreamData` frames end to end through Rust and Node. File transfers are capped at 64 MiB and now use live 256 KiB initial credit in both directions: the service consumes client upload chunks before granting more credit, and the client consumes download chunks before replenishing the service. One upload is buffered per connection to retain the 64 MiB memory ceiling; the Rust client serializes uploads separately while releasing the transport writer during credit waits, so unrelated controls remain live. Server WindowUpdate and Event frames share one strict sequence, stream registrations are removed on every response/write failure path, and nonterminal empty chunks are rejected. Spawned stdout/stderr use validated replenishment up to a 4 MiB window, a single writer capped at 128 frames/16 MiB, strict control sequencing, and bounded client channels. Late credit for a closed stream is harmless and connection drop releases stream state. The bounded pipe dispatcher accepts upload frames while dispatching up to 16 requests per connection and 64 globally, permits out-of-order responses, handles sequenced Cancel/WindowUpdate controls while operations or uploads run, applies server deadlines, and cancels every active RPC on close/break.
- Phase 5 maintenance foundation: strict path-free `PrepareUpdate`, `CommitUpdate`, `AbortUpdate`, and `PrepareUninstall` contracts are exposed through Rust and Node. The service authenticates Administrator group membership against both pipe and process tokens, requires elevation plus a configured protected maintenance-image root, validates the image's Authenticode chain, and pins its embedded signer thumbprint. Prepare writes a protected atomic pending record before closing admissions and draining every session; restart remains health/admin-only, commit requires the exact running bundle and protocol target, abort requires the opaque update ID, malformed state quarantines, and uninstall drain has its own durable restart marker. The default empty root/publisher policy denies all maintenance calls until the protected installer config is provisioned.
- Phase 5 cancellation foundation: request cancellation and deadlines propagate through dispatcher/RPC/session/VM command contexts; queue waits poll cancellation, unary exec can kill its bounded managed process, boot rootfs copy checks between chunks and cleans a cancelled startup, and new spawn/watch resources are finalized or retired on cancellation. Protocol 1.5 gives synchronous file mutations an atomic commit race: pre-commit cancellation waits for cleanup before its terminal error, while post-commit cancellation is explicitly too late and the real operation result wins. `WriteFile` stages to a random sibling and crosses commit only at atomic rename. Real Windows guest-call, cleanup-failure, process-exit, and SCM drain injection remains pending.
- Phase 5 external gates: maintenance publisher verification has compile/unit coverage but still needs an installed signed maintenance executable and protected production config. Real start/exec/spawn/file/watch/stop/update/uninstall remains unverified because this shell has no installed bundle assets or Session 0/WHPX fixture; no hardware-dependent test was forced. The broader Node integration suites reached their VM setup hooks and failed with Windows `Access is denied (os error 5)` in this non-elevated shell, leaving 24 integration tests unrun; 10 unprivileged API/package/startup tests pass. Binding-wide Clippy still has pre-existing direct-SDK findings plus the existing `lsb-sdk` `CasNbd` dead-code warning; affected service code passes strict isolated Clippy.
- Phase 6 foundation: xtask now stages only a fresh, bounded, reparse-free closed `LocalSandbox/` tree, emits exact service/bundle contracts including a quoted absolute versioned ImagePath with the sole `--service` argument, verifies AMD64 PE and pinned runtime/QEMU metadata, and creates deterministic payload/symbol ZIPs plus `SHA256SUMS`. Before enabling its engine, the SCM service performs the same adjacent installed-layout manifest/hash/contract check exposed by the standalone verifier; a failure leaves only health reporting available. Installer-required WHPX health now reflects a real platform capability query rather than `Unknown`, while the external Session 0 runtime smoke remains mandatory. Release scripts generate a full locked SPDX/license inventory, reject non-system runtime DLLs and dynamic VC runtime imports, sign/timestamp the PE and full catalog with exact subject/SHA-256 pinning, and verify catalog membership. The conditional release workflow preserves the PDB, uses static CRT, attests both ZIPs, and publishes no unsigned service when signing is disabled. The operational contract now includes the exact protected `service.json` client/maintenance root, signer, quota, and fail-closed configuration required before first start. Installer recipes, publisher rotation, release checklist, and hard external gates are in `docs/seawork-service-release.md`.
- Phase 6 external gates: the development PFX was tested only with `AllowUntrustedTestCertificate` and `SkipTimestamp`; shipping requires an organization-controlled production identity, trusted chain, RFC 3161 timestamp, and clean-machine verification. SeaWork must implement malicious-ZIP rejection, protected extraction, digest/publisher pinning, exact SCM/Event configuration, maintenance transactions, final-path reverification, and helper removal. Elevated Session 0/WHPX/two-user/crash-reboot/update-rollback-uninstall and managed-fleet EDR/GPO/proxy/VPN evidence remains release-blocking; SMB evidence gates enabling mounts rather than the narrowed initial artifact.
- Source of truth: `plan.md`; this file is the lightweight entry point and progress record

## Goal

Ship a LocalSandbox-owned, x86-64 Windows SCM service that SeaWork installs once per machine. After installation, standard users use sandboxes without UAC through the upstream Node client.

## Fixed design

- Rust service binary: `localsandbox-seawork-service.exe`
- SCM service: `LocalSandboxSeaWork`, delayed automatic start, `LocalSystem` MVP
- IPC: `\\.\pipe\LocalSandbox.SeaWork.v1`, with explicit DACL and mutual endpoint authentication
- Service calls Rust core directly; it does not host Electron or N-API
- SeaWork owns install/update/repair/uninstall; LocalSandbox owns service, protocol, authorization, lifecycle, and artifact
- Trusted state lives under protected `%ProgramData%\LocalSandbox\SeaWork`
- Caller `dataDir`, runtime paths, QEMU paths, identity claims, and cleanup manifests are not accepted
- Initial service scope is mount-free and host-port-free; enabling staged mounts or ports requires their Phase 3/WFP evidence and integration
- Disconnect stops that connection's resources; v1 has no persistent or adoptable sandboxes
- Production artifacts must be signed; there is no UAC/helper or insecure protocol fallback

## Implementation order

| Phase | State | Purpose |
| --- | --- | --- |
| 0 | Harness complete; real-machine evidence deferred | Prove Session 0 WHPX/QEMU, SMB, watches, WFP, networking, and teardown on real Windows |
| 1 | Source complete; elevated SCM verification deferred | Add protocol model, SCM shell, protected configuration, and ledger primitives |
| 2 | Shipping foundation complete; advanced endpoint hardening backlog | Add pipe identity, mutual authentication, sessions, quotas, and authorization foundation |
| 3 | Foundation complete; mounts narrowed out of initial service | Add handle-safe paths, staged mounts, privileged-resource ledger, and recovery |
| 4 | Shipping foundation complete; hard-stop/hardware integration backlog | Move sandbox lifecycle behind the service; add Job and WFP containment |
| 5 | Shipping foundation complete; hardware/elevation acceptance deferred | Complete RPC plus the upstream Rust/Node client |
| 6 | Upstream foundation complete; production/downstream/E2E gated | Build/sign/package the artifact and complete the SeaWork integration contract |

Do not expose the full privileged RPC surface before Phases 1–3 are complete. Packaging remains last.

## Start here

1. Read repository instructions.
2. Read `plan.md` section 1's “How to use this plan” map—not the entire plan.
3. For Phase 6, read sections 2, 6, 10, and 12-14. Consult section 15 only for sources needed now.
4. Recheck the branch, HEAD, worktree, toolchain, and dependency versions; preserve unrelated changes.
5. Implement the deterministic pre-sign service bundle, manifest verification, license inventory, symbols split, and release metadata before integrating any signing material.
6. Keep the Phase 0 real-machine gate in the release backlog; host ports remain disabled unless WFP isolation is proven.
7. When advancing phases, update the table and last verified commit, then follow the new phase's reading slice.

## Release-blocking evidence

- LocalSystem can run WHPX/QEMU and complete sandbox boot/exec/stop in Session 0.
- Mount and host-port requests remain deterministically unavailable in the initial service artifact; enabling either requires its Phase 3/WFP implementation and evidence.
- Crash, forced stop, and reboot reconciliation remove only provably owned resources.
- Defender/EDR, enterprise GPO, proxy/VPN, and certificate behavior is documented.
- The available production signing material is applied to the Phase 6 artifact and its chain/thumbprint are verified on a clean machine before shipping.

## Working rule

For each phase, implement the exact files/contracts in `plan.md`, add its tests, and stop only when its acceptance criteria pass. Never weaken an authorization or cleanup invariant to make a failing Windows test pass.

## Latest source tranche

- NET-02 quota-accounting tranche (2026-07-20): a fail-closed network quota book atomically enforces the fixed 32 mappings/sandbox, 64/user, 128/global, and 128 active tunnels/sandbox ceilings. Boundary tests fill every level, prove failed reservations leave counts unchanged, release and reuse one exact slot, reject duplicate sandbox reservations, prevent tunnels without a nonempty mapping reservation, and clear all tunnel/mapping state on sandbox release. The full serial service run passes 125 tests with one helper intentionally ignored, and strict scoped service Clippy passes with only documented unrelated allowances. This supersedes the quota portion of the older NET-02 status below; the book is deliberately not wired to any listener while capabilities remain disabled. NET-02 remains `implementation in progress; authenticated SeaWork-owned relay transport, quota/resource/ledger lifecycle integration, WFP logon-SID isolation, and real IPv4/IPv6 multi-user/logon/race/VPN/Firewall/EDR evidence pending`.

- NET-02 fail-closed contract/API tranche (2026-07-20): the service-backed Node start shape now preserves exact host-to-guest `ports` and normalizes `network.exposeHost` into explicit nonzero host/guest port pairs, defaulting only the omitted guest port to the requested host port. Protocol validation requires explicit unique host listener ports, unique exposed guest ports, and at most 32 combined mappings. A new client-known feature bit gates the owner-token-relay field, while the service feature mask deliberately excludes it; expose-host therefore fails negotiation before admission and host ports continue to return `PORT_ISOLATION_UNAVAILABLE`. No listener, WFP filter, direct LocalSystem loopback connection, or relay was enabled. All 28 protocol tests, 21 client tests, 7 Node Rust tests, 10 unprivileged Node API/package/startup tests, TypeScript checking, and the full serial service run (123 passed, one helper ignored) pass. NET-02 remains `implementation in progress; authenticated SeaWork-owned relay transport, per-sandbox/user/global mapping and active-tunnel quotas, service resource/ledger integration, WFP logon-SID isolation, lifecycle cleanup, and real IPv4/IPv6 multi-user/logon/race/VPN/Firewall/EDR evidence pending`.
