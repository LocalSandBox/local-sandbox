# Windows agent testing (moved)

The canonical operator guide is [`windows-testing.md`](windows-testing.md). All Windows
host setup, readiness, reset, profiles, reboot recovery, evidence, fetching, pruning,
and troubleshooting instructions live there. `scripts/windows-test/catalog.json` is
the machine-readable source of truth for suites and profile requirements.
