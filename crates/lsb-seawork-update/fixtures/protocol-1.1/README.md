# Immutable protocol 1.1 fixtures

These files are frozen N-1/helper-X wire evidence. Do not regenerate or refresh
them for an ordinary snapshot change. Their byte digests are pinned by
`tests/helper_wire_compat.rs`; changing a fixture requires an explicit helper
protocol migration and a new protocol version.
